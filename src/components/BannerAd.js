import React from 'react';

// Ad SDK not installed — banner ads disabled.
// To re-enable: install react-native-google-mobile-ads and restore SDK calls.

export default function AppBannerAd({ unitId, isPremium }) {
  return null;
}
